steps:
  - label: ":python: Test"
    commands:
      - python -m venv .venv
      - . .venv/bin/activate
      - pip install -U pip
      - pip install -r requirements.txt
      - pytest -q
    agents:
      queue: "default"